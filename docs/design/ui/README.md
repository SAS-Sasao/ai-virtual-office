# UI/UX モック（フロントモック v2）

| 項目 | 内容 |
|------|------|
| ドキュメント種別 | UI/UX 参照仕様（インタラクティブモックのミラー + 抽出仕様） |
| 正本 | claude.ai/design プロジェクト「AI Virtual Office フロントモック」<br>https://claude.ai/design/p/0eae9649-cbe2-4471-81f9-356247b230ec?file=AI+Virtual+Office+v2.dc.html |
| 取り込み日 | 2026-07-19（claude_design MCP でインポート） |
| 更新履歴 | 2026-07-19 再インポート①: ホワイトボードを「実行中タスク」表示に変更（ホバーで全件拡大パネル）<br>2026-07-19 再インポート②（v2）: ウッド調テーマへ全面刷新 / 壁バンド・フロア看板・掛け時計 / 部屋別床材 / 家具追加（ラグ・ソファ・ウォーターサーバー）/ スプライトのアウトライン化 / **秘書ナレーションバー + 操作ボタン**を追加 |
| 対応要件 | [要件定義 v0.2.1](../requirements.md) §8 画面要件 / FR-1 / FR-2 / FR-5 / FR-6 |

## ファイル構成

| ファイル | 内容 |
|---|---|
| `ai-virtual-office.dc.html` | モック本体（デザイン プロジェクト上の **`AI Virtual Office v2.dc.html`** のミラー。常に最新版を追跡し、旧版はこのリポジトリの git 履歴とデザイン側の `AI Virtual Office.dc.html` に残る）。テンプレート + 状態機械のシミュレーションロジックを含む |
| `support.js` | Claude Design のランタイム（生成物。編集しない）。`window.React` を前提とするため単体では動かない |

デザイン側の `uploads/*.png`（チャットに貼られた参考画像）はモックの仕様ではないためミラーしない。

**閲覧はデザインプロジェクト上で行う**（上記 URL。インタラクティブに動作する）。このディレクトリはバージョン管理・実装参照用のミラーであり、モックを更新したらデザインプロジェクト側を正として再インポートする（claude_design MCP の `get_file` → 本ディレクトリへ上書き → PR）。

## モックの位置づけ（AI への指示）

- **UI/UX（見た目・情報設計・インタラクション）の参照仕様として扱う**。画面レイアウト・配色・状態表現・パネル構成は、要件定義 §8 より具体的な正とする
- **実装方式の参照にはしない**。モックはオフィス部分も DOM で描いているが、実装ではオフィス描画（部屋・家具・キャラ・吹き出し）は `apps/web/game/` の Canvas 2D、外枠（ヘッダ・サイドパネル・設定画面）が React（ui-dev 担当）である。モックの DOM 構造をそのまま React に移植しないこと
- **モック内のダミーデータ（フロア名 Nebula Works / Comet Labs、キャラ名、部署構成）は仮**。実データは cc-sier-adapter が masters から生成する（FR-3）。ハードコードしない
- ピクセル素材はモック用プレースホルダー。実装時は CC0 / CC-BY 素材 + 出典表記（NFR-6）

## 抽出仕様 1: デザイントークン（v2: ウッド調）

| トークン | 値 | 用途 |
|---|---|---|
| フォント | `'DotGothic16', monospace`（Google Fonts） | 全域。ピクセル調の要 |
| 背景（ページ） | `#141017` | body |
| 背景（パネル） | `#241a10`（こげ茶） | ヘッダ・サイドパネル・カード |
| 枠線 | `#3a2415`（2〜5px、角丸なし） | 全パネル・ボタン・部屋壁 |
| 影 | `2px 2px 0 #000` 〜 `8px 8px 0 #000`（ハードシャドウ） | ピクセル調の立体感。blur は使わない |
| テキスト主 / 副 / 弱 | `#efe6d6` / `#b39b78` / `#8a7458` | — |
| アクセント（プライマリ） | `#d9a441`（アンバー） | アクティブタブ・ブランド・看板枠・ナレーションバー枠 |
| アクセント（アクション） | `#7ef29a`（緑） | 承認ボタン・保存・OK 表示 |
| アクセント（警告・待ち） | `#ffd166`（黄） | 待ちパネル・許可待ちバッジ・一時停止・フォーカスリング |
| アクセント（情報） | `#6be5ff`（シアン） | 報告ボタン・イベントログ見出し |
| 廊下・ベース床 | 木板（`repeating-linear-gradient(90deg,#7a4e30 0 26px,#734928 26px 28px)`） | オフィス外周 |
| 部屋床（floor 属性） | planks=木板明色（`#9c6a44`/`#936039`）/ carpet=緑カーペット 28px 市松（`#2f5d4a`/`#2b5644`）/ checker=クリーム市松（`#d8cfc0`/`#cbc1ae`）/ dark=`#241c14`（standby） | 部屋ごとに床材を切替（会議室=carpet、受付=checker） |
| 壁バンド | 上部 64px、`#5a3a26` + 縦影 46px 間隔、下辺 5px `#3a2415` | 本棚・フロア看板・掛け時計を配置 |
| グリッド | 32px タイル（エディタは `rgba(217,164,65,.15)` 線） | 床・エディタのスナップグリッド |

## 抽出仕様 2: キャラクター状態の表現（FR-1 / 設計 §7 と対応）

| state | ラベル | 色 | 表現 |
|---|---|---|---|
| idle | 待機 | `#b39b78` | 吹き出しなし |
| type | 編集 | `#7ef29a` | 吹き出し |
| read | 読取 | `#6be5ff` | 吹き出し |
| terminal | 端末 | `#c39bff` | 吹き出し |
| browsing | 閲覧 | `#5aa2ff` | 吹き出し |
| thinking | 思考 | `#b39b78` | 吹き出し |
| waiting | 許可待ち | `#ffd166` | 吹き出しが blink 点滅 + 待ちパネル + ヘッダバッジ |
| done | 完了 | `#ffffff` | 吹き出し |

- モック未表現の `walk` / `leave` は移動アニメーション（Canvas 実装側の状態）として補完する
- キャラ = 10×14 ピクセルのドット絵（モックでは box-shadow 生成。実装ではスプライト）+ **v2: 全周 1px の暗色アウトライン（`#1a120c`。上下左右の隣接空きピクセルに縁取り）**。表示サイズ 24×32。常時 bob（上下 2px・2.4s）。名前プレート付き
- 吹き出しは状態色の枠 + ハードシャドウ `2px 2px 0 rgba(0,0,0,.5)`
- ホバーで詳細カード: 名前(ロール) / 部署 / セッション · model / 状態 / ツール / 経過。カード枠は状態色
- サブエージェント: 会議室に spawn、親キャラと破線リンク（v2: `#e8c98a` dashed）、SubagentStop で退出
- セッション一覧クリック → 該当キャラへフォーカス（黄リング 3 秒）

## 抽出仕様 3: 画面構成（要件 §8 と対応）

| 画面 | モックでの構成 | 要件 |
|---|---|---|
| 共通ヘッダ | ブランド / フロア切替タブ / ビュー切替（オフィス・リプレイ・エディタ・設定）/ 許可待ちバッジ（blink）/ 時計 / 音 ON・OFF | §8 |
| オフィスビュー | 960×600 の Canvas 領域 + その直下に**秘書ナレーションバー**（下記）+ 右サイド 340px（セッション一覧・**待ちパネル（黄枠で最強調・確認ボタン付き）**・イベントログ 7 件） | オフィスビュー P1 / セッション一覧 P1 |
| 壁バンド（v2） | オフィス上部 64px の壁。本棚 3 台（色付き背表紙）/ 中央に**フロア看板**（組織名: 例 `NEBULA WORKS OFFICE`、アンバー枠）/ 掛け時計 | §2.1（エントランス看板に組織名） |
| 秘書ナレーションバー（v2） | 幅 968px・アンバー枠。受付キャラ名 + 直近イベントの実況（「ミナト（受付）です！ 直近のイベント: …」）。**操作ボタン 4 種**: ✔ 承認して進める（waiting 全解除）/ ■ 一旦ストップ ↔ ▶ 再開する（シミュレーション一時停止）/ 報告して（進捗レポート生成をログに記録）/ 自由に見る（フォーカス解除）。右端に待ち件数 | FR-2（待ちの見逃し防止）/ §1.1（観測・トリアージ面） |
| リプレイ | オフィスビューの上に再生バー（▶ / 日付選択 / シークバー / 位置表示 / 速度 1x·10x·60x） | リプレイ P2・FR-5 |
| エディタ | 左パレット（デスク・床・壁・植物・本棚・**ソファ**）+ 32px グリッドオーバーレイ + 「マスタ生成領域は再インポートで初期化」警告 + custom フラグ保存の説明 | エディタ P2・FR-6 |
| 設定 | 組織リポジトリ一覧（type バッジ: cc-sier / 汎用JSON）/ 通知（チャイム・タブバッジ）/ **doctor 診断 4 項目** / setup・doctor・teardown コマンド表示 | 設定 P2・§5.1 |
| フッタ | バージョン表記 + 素材出典表記欄 | NFR-6 |
| 部屋 | 部署名プレート（こげ茶地 + アンバー文字）/ ホバーでトリガー語 tooltip / standby 部署は「消灯中」オーバーレイ / **v2: 部屋ごとの床材属性**（開発系=planks、会議室=carpet、受付=checker、standby=dark）と壁 5px | §2.1 概念マッピング |
| ホワイトボード | 各フロアに 1 枚（150×44、白地 `#eef0f8`・見出し `#d94f5c`）。**実行中タスク**（idle 以外かつツール実行中のキャラ）を「名前: ツール」で最大 3 件表示、0 件なら「（なし）」。**ホバーでオフィス中央に 460px の拡大パネル**を表示し、全件を状態バッジ + 名前(ロール) + ツールで一覧（overlay z-index はキャラ・ホバーカードより上位） | オフィスビュー P1（実行中タスクの一覧性向上） |

## 抽出仕様 4: モーション

| 対象 | 値 |
|---|---|
| キャラ移動 | 1.4s linear（モック値。実装では BFS 経路 + タイル単位移動） |
| 待ち系点滅（blink） | 1〜1.2s steps(2) |
| standby 照明（pulse） | 3s |
| E2E 時 | `?e2e=1` fast-mode で全アニメーション 0 化（NFR-8。モックには未実装） |
